{
    "$schema": "https://json-schema.org/draft/2020-12/schema",
    "$id": "https://github.com/datadog/threatest/atomicRedTeam.schema.json",
    "description": "Configuration of an Atomic Red Team test case",
    "type": "object",
    "required": [
        "technique",
        "guid"
    ],
    "properties": {
        "technique": {
            "type": "string",
            "description": "MITRE ATT&CK technique ID (e.g., T1059.004)"
        },
        "guid": {
            "type": "string",
            "description": "Atomic Red Team test case GUID (auto_generated_guid from the test definition)"
        },
        "inputs": {
            "type": "object",
            "description": "Inputs for the Atomic Red Team test case",
            "additionalProperties": {
                "type": "string"
            }
        },
        "version": {
            "type": "string",
            "description": "An optional commit hash pointing to the Atomic Red Team version to use. Will default to the latest commit of the `master` branch."
        }
    }
}
